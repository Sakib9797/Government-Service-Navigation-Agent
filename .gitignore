# Secrets - never commit
.env
*.key

# Python
__pycache__/
*.py[cod]
.venv/
venv/

# Fetched/derived data: re-creatable, and data/raw is ~4MB of cached gov pages
data/raw/
data/ocr_crops/
rag/index/
data/drift/
data/feedback.jsonl

# Keep: data/services, data/taxonomy.json, data/checklists_bdr.json,
# data/source_audit.json, data/pdf_index.json, data/drafts - these are the
# project's actual output and should be versioned.

# Local editor/tooling artefacts
.vscode/
.idea/
*.local
